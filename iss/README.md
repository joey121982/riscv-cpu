# RV32I Instruction Set Simulator
This directory contains the source code for the ISS used as a "golden model" for simulation.

## Usage

The included Makefile builds both a CLI tool to directly run RV32I code, as well as a ```.so``` object to be linked into a UVM testbench. 